
let Test = {
	init(APP) {
		// return;
		// setTimeout(() => Game.start(), 100);
		// setTimeout(() => Game.loadLevel(8), 4000);
		// setTimeout(() => Game.pause(), 10000);

		// setTimeout(() => APP.content.data({ show: "level" }), 200);
	}
};
